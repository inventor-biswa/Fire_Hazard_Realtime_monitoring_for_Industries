# Core Web Framework
Flask==3.0.2
Werkzeug==3.0.1

# Computer Vision
opencv-python==4.9.0.80

# AI / YOLO Model
ultralytics>=8.1.24

# PyTorch (CPU version — works on all laptops without a GPU)
# If the laptop has an NVIDIA GPU, replace this with the CUDA version from https://pytorch.org
torch>=2.1.0
torchvision>=0.16.0

# Utilities (used internally by ultralytics)
numpy>=1.26.4
Pillow>=10.0
pandas>=2.0
matplotlib>=3.7
